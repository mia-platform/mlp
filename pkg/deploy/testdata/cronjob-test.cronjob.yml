apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: hello
  namespace: default
  annotations:
    mia-platform.eu/autocreate: 'true'
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: hello
            image: busybox
            args:
            - /bin/sh
            - -c
            - date; sleep 120
          restartPolicy: OnFailure
