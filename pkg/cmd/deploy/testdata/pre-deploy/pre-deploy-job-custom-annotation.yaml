apiVersion: batch/v1
kind: Job
metadata:
  name: pre-deploy-custom-migration
  annotations:
    mia-platform.eu/deploy: custom-pre-deploy
spec:
  template:
    metadata: {}
    spec:
      containers:
      - name: migration
        image: busybox
        args:
        - /bin/sh
        - -c
        - echo "running custom annotation migration"
      restartPolicy: Never
