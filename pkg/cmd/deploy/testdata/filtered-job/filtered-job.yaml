apiVersion: batch/v1
kind: Job
metadata:
  name: filtered-migration
  annotations:
    mia-platform.eu/deploy: pre-deploy
spec:
  template:
    metadata: {}
    spec:
      containers:
      - name: migration
        image: busybox
        args:
        - /bin/sh
        - -c
        - echo "running migration"
      restartPolicy: Never
