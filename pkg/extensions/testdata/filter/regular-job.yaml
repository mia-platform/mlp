apiVersion: batch/v1
kind: Job
metadata:
  name: regular-job
spec:
  template:
    metadata: {}
    spec:
      containers:
      - name: worker
        image: busybox
        args:
        - /bin/sh
        - -c
        - echo "working"
      restartPolicy: Never
